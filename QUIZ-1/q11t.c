#include <stdio.h>
#include <stdlib.h>
int main(int argc, char* argv)
{
    char* c = strdup(argv[1]);
    int n = strlen(c);
    for(int i = 0; i < n; i++)
    {
        printf("%s", (*(s+i));
    }
    printf("\n");


}